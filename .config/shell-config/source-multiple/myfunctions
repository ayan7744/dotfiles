# vi: ft=sh
#################################################################
# my functions
###############################################################
# cd + ls
function cs
    if count $argv > /dev/null
        builtin cd "$argv"; and la
    else
        builtin cd ~; and la
    end
end

function opensh --argument name
	/usr/bin/nvim $HOME/bin/$name
end
